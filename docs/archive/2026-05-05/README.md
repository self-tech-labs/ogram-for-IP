# 2026-05-05 development archive

This directory preserves historical planning and handoff material from the original developer package:

- `Documentation_Developpeur_Swiss_Trademark_Deposit.docx` is the original developer document;
- `handoff/` contains the ten-part Markdown handoff; and
- `legacy-skill/SKILL.md` is the earlier, long-form skill source.

The redundant generated `swiss-trademark-deposit.skill` archive was removed. It duplicated the retained Markdown skill and can be regenerated if ever needed.

These files are historical context, not current runtime documentation. Current architecture, provenance, host support, and maintenance guidance live directly under `docs/`. The five ingestion inputs were separated into `data-sources/2026-05-05/` so historical design material cannot be confused with runtime data provenance.
